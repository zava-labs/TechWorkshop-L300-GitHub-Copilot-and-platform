name: Build and Deploy to Azure App Service

on:
  push:
    branches: [ main ]
    paths: [ 'src/**' ]
  pull_request:
    branches: [ '*' ]
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Log in to Azure
      uses: azure/login@v2
      with:
        creds: ${{ secrets.AZURE_CREDENTIALS }}

    - name: Log in to Azure Container Registry
      run: |
        az acr login --name ${{ vars.AZURE_CONTAINER_REGISTRY }}

    - name: Build and push Docker image
      run: |
        az acr build \
          --registry ${{ vars.AZURE_CONTAINER_REGISTRY }} \
          --image zavastore:${{ github.sha }} \
          --image zavastore:latest \
          --file Dockerfile \
          ./src

    - name: Deploy to Azure App Service
      run: |
        az webapp config container set \
          --name ${{ vars.AZURE_APP_SERVICE_NAME }} \
          --resource-group ${{ vars.RESOURCE_GROUP_NAME }} \
          --docker-custom-image-name ${{ vars.AZURE_CONTAINER_REGISTRY }}.azurecr.io/zavastore:${{ github.sha }} \
          --docker-registry-server-url https://${{ vars.AZURE_CONTAINER_REGISTRY }}.azurecr.io

    - name: Restart App Service
      run: |
        az webapp restart \
          --name ${{ vars.AZURE_APP_SERVICE_NAME }} \
          --resource-group ${{ vars.RESOURCE_GROUP_NAME }}
